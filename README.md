To create an virtual env and activate in Windows:
```sh
python -m venv .venv
.venv/Scripts/activate
```

To install all the packages required in this project:
```sh
pip install -r requirements.txt
```

To create an executable, navigate to the script's directory and run:
```sh
pyinstaller main.py
```
